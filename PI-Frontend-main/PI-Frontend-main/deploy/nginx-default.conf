server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA — API calls use absolute apiUrl from Angular environment (or terminate TLS at ingress and proxy /api here).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
